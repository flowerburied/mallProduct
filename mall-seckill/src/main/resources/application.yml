spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.18.128:8848
  application:
    name: mall-seckill
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  redis:
    host: 192.168.18.128
    port: 6380
  session:
    store-type: redis
  thymeleaf:
    cache: false

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 25000